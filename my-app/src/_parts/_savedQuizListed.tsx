"use client";

import { Quiz } from "@/providers/QuizgeekProvider";
import { useState, useEffect } from "react";
//show the saved Quizzes. Only show the one that has quizzes saved.
//
export const SavedQuizzesListed = (props: Quiz[]) => {
    return (
        <></>
    )
};
