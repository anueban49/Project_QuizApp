import type { RequestHandler } from "express";
import { nanoid } from "nanoid";

//this route has to get the article generation request and assign an id
export const createConversation: RequestHandler = async(req, res ) => {

}